import React, { Component } from 'react';

const Button = (props) => {
  return <button onClick= { props.onNewAragmatiki } className="addButton btn btn-default" type="button">
        +
    </button>
}


export default Button;
